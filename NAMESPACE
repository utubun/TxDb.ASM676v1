import(AnnotationDbi)
import(GenomicFeatures)

### Don't export TxDb.Paeruginosa.PAO1 (the object defined in this
### package): it is created and dynamically exported at load time (refer
### to R/zzz.R for the details).


